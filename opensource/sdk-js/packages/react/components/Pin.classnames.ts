import { cordifyClassname } from '../common/cordifyClassname.js';

export const pinContainer = cordifyClassname('pin-container');
export const pinClassnamesDocs = {
  [pinContainer]: 'Applied to the container div. This class is always present.',
};
