mutation UpdateApplicationS3BucketSecretMutation(
  $applicationID: UUID!
  $id: UUID!
  $keyID: String!
  $keySecret: String!
) {
  updateCustomS3BucketSecret(
    applicationID: $applicationID
    id: $id
    keyID: $keyID
    keySecret: $keySecret
  ) {
    success
  }
}
