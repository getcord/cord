import { cordifyClassname } from 'common/ui/style.ts';

export const timestamp = cordifyClassname('timestamp');

export const timestampClassnamesDocs = {
  [timestamp]: 'Applied to the p containing the timestamp',
};
